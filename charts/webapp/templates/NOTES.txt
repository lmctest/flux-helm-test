MariaDB can be accessed via port 3306 on the following DNS name from within your cluster:
{{ template "webapp.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local

{{ if .Values.usePassword -}}
To get the root password run:

    MARIADB_ROOT_PASSWORD=$(kubectl get secret --namespace {{ .Release.Namespace }} {{ template "webapp.fullname" . }} -o jsonpath="{.data.webapp-root-password}" | base64 --decode)

{{ if .Values.webappUser -}}
To get the password for "{{ .Values.webappUser }}" run:

    MARIADB_PASSWORD=$(kubectl get secret --namespace {{ .Release.Namespace }} {{ template "webapp.fullname" . }} -o jsonpath="{.data.webapp-password}" | base64 --decode)
{{- end }}
{{- end -}}

To connect to your database:

1. Run a pod that you can use as a client:

    kubectl run {{ template "webapp.fullname" . }}-client --rm --tty -i {{ if .Values.usePassword }}--env MARIADB_ROOT_PASSWORD=$MARIADB_ROOT_PASSWORD{{ end }} --image bitnami/webapp --command -- bash

2. Connect using the mysql cli, then provide your password:
    mysql -h {{ template "webapp.fullname" . }} {{- if .Values.usePassword }} -p$MARIADB_ROOT_PASSWORD{{ end }}
